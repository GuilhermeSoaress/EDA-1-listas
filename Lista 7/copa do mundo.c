#include <stdio.h>

int main(){
    int m[15], n[15];
    
    for (int i=0; i<15; i++){
        scanf("%d %d", &m[i], &n[i]);
    }

    for (int i=0; i<8; i++){
        if (m[i] > n[i]){
        
        }
    }
}